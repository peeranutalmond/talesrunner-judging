# คู่มือการใช้งานระบบ Artventure Judge Console (Tales Runner Edition)
> **ระบบตัดสินผลงานการประกวดแฟนอาร์ต (Fan-Art Judging System)**  
> ผสานงานดีไซน์สไตล์ **Tales Runner & Gartic Phone Arcade POP** พร้อมโครงสร้าง **Frontend & Backend** เต็มรูปแบบ

---

## 📑 สารบัญ
1. [ภาพรวมโครงสร้างระบบ (Architecture Overview)](#1-ภาพรวมโครงสร้างระบบ-architecture-overview)
   - [Frontend Architecture](#frontend-architecture)
   - [Backend Architecture](#backend-architecture)
2. [การติดตั้งและเริ่มใช้งานระบบ (Getting Started)](#2-การติดตั้งและเริ่มใช้งานระบบ-getting-started)
3. [การเข้าสู่ระบบครั้งแรก (Login & Lobby)](#3-การเข้าสู่ระบบครั้งแรก-login--lobby)
4. [การจัดการรายชื่อกรรมการ (Judge Management)](#4-การจัดการรายชื่อกรรมการ-judge-management)
   - [การเพิ่มกรรมการใหม่ (Add Judge)](#การเพิ่มกรรมการใหม่-add-judge)
   - [การลบกรรมการออกจากระบบ (Delete Judge)](#การลบกรรมการออกจากระบบ-delete-judge)
   - [การเปิด/ปิด และกำหนดสิทธิ์ Required / Optional](#การเปิดปิด-และกำหนดสิทธิ์-required--optional)
5. [การจัดการผลงานประกวด (Submissions Management)](#5-การจัดการผลงานประกวด-submissions-management)
   - [การเพิ่มผลงานทีละชิ้น](#การเพิ่มผลงานทีละชิ้น)
   - [การนำเข้าผลงานแบบกลุ่มผ่าน Google Forms CSV](#การนำเข้าผลงานแบบกลุ่มผ่าน-google-forms-csv)
   - [การแบ่งหมวดหมู่ (Artwork Tracks)](#การแบ่งหมวดหมู่-artwork-tracks)
6. [การตั้งค่าเกณฑ์การให้คะแนน (Scoring Criteria)](#6-การตั้งค่าเกณฑ์การให้คะแนน-scoring-criteria)
7. [ขั้นตอนการตัดสินสำหรับกรรมการ (Judge Experience)](#7-ขั้นตอนการตัดสินสำหรับกรรมการ-judge-experience)
8. [การดูผลการตัดสินและโพเดียมอันดับ (Visual Rankings & Podium)](#8-การดูผลการตัดสินและโพเดียมอันดับ-visual-rankings--podium)
9. [การล็อกผลการแข่งขันและส่งออกข้อมูล (Lock & Export)](#9-การล็อกผลการแข่งขันและส่งออกข้อมูล-lock--export)
10. [คำสั่งสำหรับนักพัฒนา (Developer Commands)](#10-คำสั่งสำหรับนักพัฒนา-developer-commands)

---

## 1. ภาพรวมโครงสร้างระบบ (Architecture Overview)

ระบบ Artventure แบ่งออกเป็น 2 ฝั่งหลักอย่างชัดเจน:

```
                  ┌────────────────────────────────────────┐
                  │          USER INTERFACE (UI)           │
                  │   Tales Runner & Gartic Phone POP      │
                  └───────────────────┬────────────────────┘
                                      │ HTTP / Server Actions
                                      ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                          FRONTEND (Next.js 16)                            │
│  - App Router (React 19 Server & Client Components)                       │
│  - Tailwind CSS 4 + Custom 3D Arcade Effects (.arcade-btn, .comic-card)   │
│  - Official Tales Runner Assets (Logos, Stat Icons, Character Avatars)    │
│  - Google Fonts: Rowdies (Arcade Typography) & Prompt (Thai Typography)   │
│  - Client-side optimistic responsiveness & Autosave                       │
└─────────────────────────────────────┬─────────────────────────────────────┘
                                      │ Secure Mutations
                                      ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                           BACKEND & DATA LAYER                            │
│  - Next.js Server Actions (Type-safe Zod validation & RBAC security)      │
│  - Audit Logging Engine (บันทึกประวัติการกระทำทุกขั้นตอนแบบ immutable)    │
│  - Image Resolving & Upload Handler (Google Drive & Direct Upload)        │
│  - Session Auth (Signed HTTP-only Cookies + PIN Rate Limiting)            │
│  - Database:                                                              │
│      • Local/Dev: PGlite (Zero-config embedded PostgreSQL in data/pglite) │
│      • Production: Supabase PostgreSQL (via DATABASE_URL & RLS)           │
└───────────────────────────────────────────────────────────────────────────┘
```

### Frontend Architecture
- **Tech Stack:** Next.js 16.3.5 (App Router), React 19, Tailwind CSS 4, Lucide React Icons.
- **Visual Design:** โทนสีและสไตล์เกมสตรีทอาร์เคด (POP Arcade) นำแรงบันดาลใจจากหน้าเว็บทางการของ **Tales Runner** (`talesrunner.thehof.gg`) และมินิเกม **Gartic Phone**
  - ปุ่ม 3D กดนูนหนาพร้อมเงาขอบคม (`arcade-btn`)
  - การ์ดสไตล์หนังสือการ์ตูนคอมมิคขอบดำหนา (`comic-card`)
  - ฟอนต์ประจำเกม: `Rowdies` สำหรับหัวข้อตัวเลขภาษาอังกฤษ และ `Prompt` สำหรับภาษาไทย
  - มาตรวัดพลังดาว **Star Power Scoreboard** และหลอดความคืบหน้ารางรถแข่ง **Checkered Track Gauge**
  - รองรับหน้าจอคอมพิวเตอร์และมือถืออย่างสมบูรณ์ (Responsive & Mobile-first)

### Backend Architecture
- **Tech Stack:** TypeScript 6, Node.js, Next.js Server Actions, Zod Schema Validation, Bcrypt.js (แฮช PIN อย่างปลอดภัย).
- **Data Persistence:**
  - รันในเครื่องทันทีไม่ต้องติดตั้ง Postgres ด้วย **PGlite** (ข้อมูลจะถูกจัดเก็บทนทานไว้ในโฟลเดอร์ `data/pglite/`).
  - เมื่อพร้อมขึ้น Production เพียงระบุ `DATABASE_URL` ระบบจะเชื่อมต่อเข้ากับ PostgreSQL หรือ Supabase โดยอัตโนมัติ.
- **Security & Integrity:**
  - ระบบตรวจสอบบทบาทผู้ใช้ (RBAC): `SUPER_ADMIN`, `ADMIN`, `JUDGE`
  - การประเมินคะแนนแบบ Blind Judging (กรรมการแต่ละคนไม่สามารถเห็นคะแนนของผู้อื่นได้จนกว่าจะปิดการประกวด)
  - ระบบป้องกันการเดารหัสผ่าน PIN (PIN Throttling / Rate-limiting)
  - ระบบ Audit Logs จดบันทึกทุกการแก้ไขคะแนน การสร้าง และการลบผู้ใช้

---

## 2. การติดตั้งและเริ่มใช้งานระบบ (Getting Started)

1. เปิด Terminal หรือ Command Prompt ในโฟลเดอร์โปรเจกต์:
   ```bash
   cd C:\Users\almon\Documents\ChatGPT\DD
   ```

2. ติดตั้ง Dependencies (ถ้ายังไม่ได้ติดตั้ง):
   ```bash
   pnpm install
   # หรือ npm install
   ```

3. รันเซิร์ฟเวอร์สำหรับ Development:
   ```bash
   pnpm dev
   # หรือ npm run dev
   ```

4. เปิดเบราว์เซอร์ไปที่:
   ```
   http://localhost:3000
   ```

---

## 3. หน้าห้องรับรองนักวิ่งและเข้าสู่ระบบ (Gartic Phone POP Lobby)

หน้าเข้าสู่ระบบได้รับการออกแบบใหม่ในสไตล์ **Gartic Phone Party Lobby** (`https://garticphone.com/lobby`) ผสานเอกลักษณ์ของ **Tales Runner**:

1. **สลับโหมดห้อง (Mode Tabs):**
   - **🏃 โหมดกรรมการ (Judge Lobby):** สำหรับกรรมการเข้าตรวจผลงาน 69 ชิ้น
   - **👑 โหมดแอดมิน (Admin Console):** สำหรับผู้ดูแลระบบจัดการทีมกรรมการและดูสรุปผล
2. **Character Spotlight & Carousel:**
   - กรอบรูปตัวละครทรงกลมขนาดใหญ่สไตล์การ์ตูน พร้อมปุ่มลูกศร **`<`** และ **`>`** สลับเปลี่ยนตัวละครได้อย่างลื่นไหล
   - มีแถบรูปตัวละครขนาดเล็กด้านล่าง สามารถแตะเลือกตัวละครที่ต้องการได้ทันที
3. **ปุ่มยักษ์ START ทันทีใน 1 คลิก (1-Click Instant Play):**
   - เพียงเลือกตัวละคร แล้วกดปุ่ม 3D สีเขียวขนาดใหญ่ **"START เริ่มตัดสินทันที 1-Click 🎮"** (สำหรับโชวอน หรือกรรมการ)
   - หรือกดปุ่ม 3D สีทอง **"MANAGE เข้าแอดมินทันที 1-Click 👑"** (สำหรับ Admin01)
   - ระบบจะใส่รหัส PIN อัตโนมัติ (`1234` สำหรับโชวอน, `2468` สำหรับ Admin01) และเข้าสู่ระบบทันทีโดยไม่ต้องพิมพ์รหัสซ้ำ
4. **การใส่รหัส PIN อื่นๆ (Custom PIN):**
   - หากกรรมการท่านอื่นมีรหัส PIN เฉพาะ สามารถกดที่ปุ่ม *"ต้องการเปลี่ยนหรือใส่รหัส PIN เอง? (คลิกที่นี่)"* เพื่อเปิดช่องใส่ PIN 4–6 หลักได้ตามต้องการ

---

## 4. การจัดการรายชื่อกรรมการ (Judge Management)

เข้าใช้งานผ่านเมนูด้านบน: **"ทีมกรรมการ"** หรือไปที่ URL `/admin/judges`

### การเพิ่มกรรมการใหม่ (Add Judge)
1. ในหน้าทีมกรรมการ คลิกปุ่มสีเขียว **"+ เพิ่มกรรมการใหม่"** (มุมขวาบน)
2. กรอกข้อมูลกรรมการ:
   - **ชื่อกรรมการ:** ระบุชื่อที่ต้องการให้แสดง (เช่น อาจารย์สมเกียรติ, พี่บาส TR)
   - **PIN เข้าสู่ระบบ (4–6 หลัก):** ตั้งรหัสผ่านตัวเลขสำหรับให้กรรมการท่านนี้ล็อกอิน (เช่น `1111`, `9999`)
   - **อีเมล:** (ไม่บังคับ)
3. **เลือกรูปโปรไฟล์ตัวละคร:**
   - สามารถคลิกเลือกตัวละครยอดฮิตจาก Tales Runner ได้ทันที 1 คลิก:
     - **Chowon** (โชวอน)
     - **Lina** (ลีน่า)
     - **Mingming** (หมิงหมิง)
     - **Bigbo** (บิ๊กโบ)
     - **DnD** (ดีเอ็นดี)
     - **Maki** (มากิ)
     - **Rough** (ราฟ)
     - **Bada** (บาดา)
   - หรือหากต้องการใช้รูปจริง ให้คลิกปุ่ม **"Upload"** ในช่องอัปโหลดรูปภาพเพื่อเลือกรูปภาพจากเครื่อง หรือวางลิงก์รูปภาพ
4. คลิกปุ่ม **"ยืนยันเพิ่มกรรมการ"**
5. กรรมการคนใหม่จะปรากฏบนหน้ารายชื่อทันที และจะไปปรากฏในหน้า Login Lobby ให้นักวิ่งท่านนั้นล็อกอินได้ทันที!

### การลบกรรมการออกจากระบบ (Delete Judge)
หากต้องการนำกรรมการออก หรือลบกรรมการที่เพิ่มไว้สำหรับทดสอบ:
1. เลื่อนไปที่การ์ดของกรรมการท่านที่ต้องการลบ
2. คลิกที่ข้อความสีแดง **"🗑️ ลบกรรมการนี้ออกจากระบบ"** ด้านล่างของการ์ด
3. ระบบจะเปิดแถบคำเตือนสีแดง เพื่อยืนยันความปลอดภัย
4. คลิกปุ่มสีแดง **"ยืนยันลบกรรมการนี้"**
5. ระบบจะทำการลบข้อมูลกรรมการ คิวตรวจ และคะแนนที่เกี่ยวข้องออกจากระบบอย่างสมบูรณ์ และบันทึกประวัติไว้ใน Audit Log

### การเปิด/ปิด และกำหนดสิทธิ์ Required / Optional
- **เปลี่ยนเป็น Optional / Required:**
  - `REQUIRED`: กรรมการท่านนี้จำเป็นต้องตรวจครบทุกชิ้นงาน ระบบจึงจะสรุปผลและปลดล็อกคะแนนได้
  - `OPTIONAL`: กรรมการร่วมตรวจ คะแนนจะถูกนำมาคำนวณเฉลี่ยแต่ไม่บล็อกการสรุปผลหากตรวจไม่ครบ
- **เปิดใช้งาน / ปิดใช้งาน (Disable / Enable):**
  - สามารถปิดการเข้าใช้งานชั่วคราวได้โดยไม่ต้องลบประวัติ

---

## 5. การจัดการผลงานประกวด (Submissions Management)

เข้าใช้งานผ่านเมนู **"ผลงาน"** หรือ URL `/admin/submissions`

### การเพิ่มผลงานทีละชิ้น
- คลิก **"+ เพิ่มผลงาน"**
- กรอกรหัสผลงาน, ชื่อผลงาน, ชื่อศิลปิน/ผู้ส่ง, ไอดีผู้เล่น Tales Runner
- อัปโหลดไฟล์ภาพ (รองรับ `.png`, `.jpg`, `.webp` สูงสุด 15 MB) หรือวางลิงก์ Google Drive
- เลือกหมวดหมู่ (Track)

### การนำเข้าผลงานแบบกลุ่มผ่าน Google Forms CSV
1. ไปที่ `/admin/submissions/import`
2. อัปโหลดไฟล์ CSV ที่ดาวน์โหลดจาก Google Forms
3. ระบบมีฟังก์ชัน **Smart Mapping** จับคู่อัตโนมัติ:
   - รหัสประจำตัวผู้เล่น
   - ชื่อที่แสดง
   - ลิงก์รูปภาพ Google Drive (ระบบจะทำการแปลงเป็นลิงก์รูปภาพความละเอียดสูงที่พร้อมแสดงผลในเว็บทันที)
4. ตรวจสอบความถูกต้องในหน้า Preview แล้วกดยืนยันการนำเข้า

### ข้อมูลผลงานจริงจาก Google Sheet (69 ชิ้นงาน)
ระบบได้รับการนำเข้าผลงานจริงทั้งหมด 69 ผลงาน จากชีตกิจกรรม `[ส่งผลงาน] Tales Artventure : Animal Village with Friends`:
- **ลิงก์ Google Sheets:** [คลิกเปิด Google Sheet](https://docs.google.com/spreadsheets/d/1QXBy7Inb437CbXjA9C9HjSuewdmtZ2-HLqJoVIZ4af8/edit?gid=1113618246#gid=1113618246)
- **สถิติการนำเข้า:** รวมทั้งหมด 69 รายการ
  - 🖥️ **Digital Art:** 53 ผลงาน
  - ✏️ **Traditional Art:** 16 ผลงาน
- **ข้อมูลที่บันทึก:** นามปากกา, บัญชี Social, Tales Runner Username, UID ในเกม, ชื่อผลงาน, และลิงก์ไฟล์ภาพ Google Drive
- **สคริปต์ซิงก์ข้อมูล:** สามารถรันคำสั่ง `node scripts/inspect-sheet.mjs` เพื่อดึงข้อมูลล่าสุดจาก Google Sheets และซิงก์เข้าสู่ฐานข้อมูลได้โดยอัตโนมัติ

### หน้าคู่มือการใช้งานบนเว็บไซต์ (`/guide`)
- มีหน้าคู่มือภาษาไทยเต็มรูปแบบบนเว็บที่ `/guide` ซึ่งมีปุ่มลิงก์จากหน้า Login และแถบ Header ด้านบนในห้องตัดสิน
- รวบรวมขั้นตอนสำหรับกรรมการ, หน้าที่ของแอดมิน, เกณฑ์คะแนน 5 ด้าน, ปุ่มลัดคีย์บอร์ด, และวิธีแก้ไขกรณีภาพ Google Drive ไม่แสดงผล

### การแบ่งหมวดหมู่ (Artwork Tracks)
เข้าเมนู `/admin/categories` เพื่อกำหนดสายการประกวด เช่น:
- ✏️ **วาดบนกระดาษ (Traditional Art)**
- 🖥️ **วาดด้วยเครื่องมือดิจิทัล (Digital Art)**
- หรือสร้างหมวดหมู่ใหม่ตามธีมกิจกรรม พร้อมเลือกไอคอนและสีประจำหมวด

---

## 6. การตั้งค่าเกณฑ์การให้คะแนน (Scoring Criteria)

เข้าเมนู `/admin/criteria`

- ระบบใช้มาตรฐาน **คะแนนรวมเต็ม 100 คะแนน** (100-Point Rule)
- เกณฑ์มาตรฐานเริ่มต้น:
  1. Theme & Concept (30 คะแนน)
  2. Composition & Storytelling (25 คะแนน)
  3. Creativity (20 คะแนน)
  4. Technique & Completion (15 คะแนน)
  5. Character Expression (10 คะแนน)
- สามารถปรับเปลี่ยนชื่อเกณฑ์ คำอธิบาย และน้ำหนักคะแนนได้ โดยผลรวมต้องเท่ากับ 100 เสมอ
- เมื่อแก้ไขเสร็จ ให้พิมพ์ยืนยัน `CREATE VERSION` เพื่อสร้างเวอร์ชันใหม่โดยไม่ทำลายข้อมูลคะแนนเดิม

---

## 7. ขั้นตอนการตัดสินสำหรับกรรมการ (Judge Experience)

เมื่อกรรมการล็อกอินเข้าสู่ระบบด้วยรหัส PIN ของตนเอง:

1. **หน้ารันสนามตรวจ (`/judge`):**
   - ด้านบนแสดง **Runner HUD** แสดงโปรไฟล์ของกรรมการ, หลอด XP ตรวจผลงาน และจำนวนดาวสะสม
   - มีปุ่มลัดบนแถบ Header ด้านขวา: **"📖 วิธีใช้"**, **"📋 Review"**, **"👑 ไปหลังบ้าน"** (สลับเข้าโหมดแอดมินทันที 1-Click), และ **"🚪 Logout"** (ออกจากระบบ)
   - **เลือกสนามแข่งขัน (Track Selector):** มีปุ่มแท็บแยกสายการตัดสินชัดเจน:
     - 🏁 **ทุกสนาม (69 ผลงาน)**
     - 🖥️ **Digital Art (53 ผลงาน)**
     - ✏️ **Traditional Art วาดบนกระดาษ (16 ผลงาน)**
     ช่วยให้กรรมการตรวจแยกทีละสายงานได้สะดวกรวดเร็ว ไม่ต้องตรวจปนกัน
   - **แถบ Checkpoint วงกลมระบุสถานะ (ข้าม-ย้อนกลับได้ทันที):**
     - 🟢 **สีเขียว (มีเครื่องหมาย ✓):** ผลงานที่ตรวจและบันทึกคะแนนเรียบร้อยแล้ว
     - ⚪ **สีขาว (ตัวเลขธรรมดา):** ผลงานที่ยังไม่ได้ตรวจ หรือเผลอกดข้ามไป
     - 🔵 **สีฟ้า Cyan เรืองแสง:** ผลงานชิ้นปัจจุบันที่กำลังเปิดดูอยู่
     - 🚩 **ไอคอนธงแดง:** ผลงานที่กรรมการกดติดธงไว้
     - **คลิกกระโดด:** คลิกที่วงกลมหมายเลขใดก็ได้เพื่อข้ามไปตรวจงานนั้นทันที (ระบบบันทึกคะแนนงานปัจจุบันอัตโนมัติ)
     - **ปุ่มทางลัด "⚡ ไปจุดที่เผลอข้าม (#X)":** คลิกเพื่อวาร์ปไปยังงานชิ้นแรกที่ยังไม่ได้ตรวจได้อย่างแม่นยำ
   - แสดงภาพผลงานประกวดขนาดใหญ่ กรรมการสามารถคลิกเพื่อซูมดูขนาดภาพต้นฉบับคมชัดเต็มจอได้
   - แสดงข้อมูลผลงาน (หมวดหมู่, ชื่อผลงาน) โดยปิดบังข้อมูลส่วนตัวเพื่อความยุติธรรม (Blind Mode)
2. **การให้คะแนน:**
   - แต่ละเกณฑ์จะมีมาตรวัดคะแนนและปุ่มกด **`+`** / **`-`** สไตล์เกมอาร์เคด
   - มีปุ่มทางลัดคะแนนด่วน (Quick Multipliers) เช่น 25%, 50%, 75%, 100%
   - ช่องคอมเมนต์สไตล์บอลลูนคำพูดการ์ตูน สำหรับใส่ข้อคิดเห็นถึงผลงาน
   - ปุ่มกด **"ติดธงไว้ดูซ้ำ (Flag)"** สำหรับชิ้นงานที่ต้องการกลับมาพิจารณาอีกครั้ง
3. **การเข้าสู่ผลงานถัดไป:**
   - กดปุ่ม **"CLEAR & NEXT 🚀"** เมื่อให้คะแนนครบทุกเกณฑ์
   - กดปุ่ม **"ข้ามไปก่อน ⏭️"** หากยังไม่พร้อมให้คะแนนชิ้นนี้ (วงกลมบน Checkpoint จะยังคงเป็นสีขาว)
   - ระบบจะทำแอนิเมชัน Checkpoint Transition (220ms) สลับไปยังผลงานชิ้นถัดไปอย่างลื่นไหล
4. **หน้าสรุปผลงาน Stage Clear (`/judge/review`):**
   - เมื่อตรวจครบหรือต้องการตรวจทาน กรรมการสามารถเข้าหน้าทบทวนเพื่อดูรายชื่อผลงานทั้งหมดที่ตนเองตรวจไปแล้ว
   - สามารถกรองดูเฉพาะสนาม Digital Art หรือ Traditional Art ได้
   - สามารถกดกลับไปแก้ไขคะแนนของผลงานใดก็ได้ตลอดเวลาตราบใดที่แอดมินยังไม่ล็อกผลการตัดสิน
5. **ขั้นตอนสุดท้าย: จัดอันดับ & ติ๊กเลือก Top 3 - 5 ผลงานที่ชื่นชอบที่สุด (Final Stage · Top Picks):**
   - เมื่อให้คะแนนครบทุกภาพแล้ว ระบบจะพากรรมการเข้าสู่โหมด **Final Top Picks** โดยอัตโนมัติ (หรือคลิกปุ่ม **"⭐ Top Picks"** ที่เมนูด้านบน)
   - **เรียงรูปทุกรูปใหม่อีกรอบ:** ระบบจะจัดเรียงรูปทั้งหมดตาม **คะแนนที่คุณให้ไว้ (มาก ➔ น้อย)** เป็นค่าเริ่มต้น เพื่อให้เห็นผลงานที่คุณให้คะแนนสูงสุดอยู่ด้านบน หรือเลือกเรียงตามหมายเลขและแยกตามสนามได้
   - **ติ๊กเลือกผลงานที่ชื่นชอบ:** กดปุ่ม **"⭐ ติ๊กเลือกเป็น Top Pick"** ใต้ภาพที่ชอบ โดยระบบจะติดเหรียญ 🥇 อันดับ 1, 🥈 อันดับ 2, 🥉 อันดับ 3, ⭐ อันดับ 4, ⭐ อันดับ 5 ให้ตามลำดับ
   - **กำหนดจำนวนผลงานได้ (Fix ได้):** มีปุ่มให้เลือกโหมดจำนวนผลงานตามที่ต้องการ:
     - `3 - 5 อัน (ยืดหยุ่น)`: เลือกได้ตั้งแต่ 3 ถึง 5 ผลงาน
     - `ล็อก 3 อัน 🔒`: บังคับเลือก 3 ผลงานพอดี
     - `ล็อก 4 อัน 🔒`: บังคับเลือก 4 ผลงานพอดี
     - `ล็อก 5 อัน 🔒`: บังคับเลือก 5 ผลงานพอดี
   - **แท่นรางวัล Top Picks Showcase:** แถบด้านบนจะแสดงภาพผลงานที่เลือกไว้ กรรมการสามารถกดปุ่ม **`▲` ขึ้น** หรือ **`▼` ลง** เพื่อสลับอันดับความชอบได้อย่างอิสระ
   - **บันทึกผลงาน:** เมื่อเลือกครบตามเกณฑ์ ปุ่มจะเปลี่ยนเป็นสีเขียว **"💾 บันทึก Top Picks (X อัน) 🚀"** กดบันทึกเพื่อส่งข้อมูลเข้าสู่ระบบหลังบ้านสำหรับนำไปมอบ **รางวัลขวัญใจกรรมการ (Judges' Choice Awards)**

---

## 8. การดูผลการตัดสินและโพเดียมอันดับ (Visual Rankings & Podium)

เข้าเมนู **"ผลการตัดสิน"** หรือ URL `/admin/results`

หน้านี้ได้รับการปรับปรุงใหม่เพื่อความสะดวกสูงสุดในการดูผลการประกวด:
1. **Top 3 Podium Showcase (แท่นรับเหรียญรางวัล):**
   - แสดงการ์ด 3 อันดับแรกแยกตามแต่ละหมวดหมู่ (Traditional, Digital)
   - 🥇 **อันดับที่ 1 (Champion):** แสดงรูปภาพผลงานขนาดใหญ่เด่นชัดพร้อมเหรียญทอง
   - 🥈 **อันดับที่ 2 (Silver):** แสดงรูปภาพพร้อมเหรียญเงิน
   - 🥉 **อันดับที่ 3 (Bronze):** แสดงรูปภาพพร้อมเหรียญทองแดง
   - แสดงชื่อภาพ, ชื่อศิลปิน, และคะแนนรวมเฉลี่ย
2. **Visual Leaderboard Table (ตารางอันดับแบบเห็นรูปภาพ):**
   - ทุกแถวของตารางอันดับจะแสดง **ภาพตัวอย่างผลงาน (Artwork Thumbnail)** ชัดเจน ไม่ต้องจำเพียงชื่อรหัสผลงาน
   - คลิกที่รูปภาพเพื่อเปิดดูผลงานขนาดเต็ม
   - คลิกที่คะแนนเพื่อดูแจกแจงคะแนนของกรรมการแต่ละท่านแบบละเอียด

---

## 9. การล็อกผลการแข่งขันและส่งออกข้อมูล (Lock & Export)

1. เมื่อกรรมการทุกคนตรวจครบแล้ว ให้ตรวจสอบที่หน้า `/admin/progress`
2. ไปที่หน้า `/admin/results` เลื่อนลงไปที่กล่อง **Lock Results**
3. พิมพ์คำว่า `LOCK` แล้วกดปุ่มยืนยัน เพื่อป้องกันไม่ให้มีการแก้ไขคะแนนเพิ่มเติม
4. ไปที่เมนู **"ส่งออกข้อมูล (Export)"** (`/admin/export`):
   - ดาวน์โหลดรายงานจัดอันดับ (Leaderboard)
   - ดาวน์โหลดคะแนนดิบรายเกณฑ์ (Raw Criteria Breakdown)
   - ดาวน์โหลดประวัติการให้คะแนนและ Audit Log
   - รองรับรูปแบบ **Excel (.xlsx)**, **CSV**, และ **JSON**

---

## 10. คำสั่งสำหรับนักพัฒนา (Developer Commands)

สามารถรันคำสั่งเหล่านี้ได้จาก Command Prompt หรือ Terminal:

| คำสั่ง | รายละเอียด |
| :--- | :--- |
| `pnpm dev` | เปิดเซิร์ฟเวอร์ Local Development (`localhost:3000`) |
| `pnpm build` | ทำการ Compile และ Build โปรเจกต์สำหรับ Production |
| `pnpm lint` | ตรวจสอบ Code Quality และ ESLint rules (ต้องได้ 0 errors) |
| `pnpm typecheck` | ตรวจสอบความถูกต้องของ TypeScript ทั้งโปรเจกต์ (`tsc --noEmit`) |
| `node scripts/clean-demo.mjs` | สคริปต์ทำความสะอาดลบกรรมการเดโมเดิม (Venus/Zelfur) ออกจาก local db |

---

> 🏆 **Artventure Judge Console** — พร้อมใช้งานสำหรับการประกวดแฟนอาร์ตอย่างเต็มรูปแบบ ทั้งความสวยงามตามสไตล์ Tales Runner และความเสถียรของระบบจัดการข้อมูลหลังบ้าน!
