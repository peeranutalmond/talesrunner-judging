"use client";

import { useMemo, useState } from "react";
import { ArrowDown, ArrowUp, Copy, Plus, Trash2 } from "lucide-react";
import { Button, Input } from "@/components/ui";
import { createCriteriaVersionAction } from "@/lib/services/admin-actions";

type Item={name:string;description:string;maxScore:number};
const presets:Record<string,Item[]>={
  "Fan Art":[{name:"Theme & Concept",description:"ความสอดคล้องกับธีม",maxScore:30},{name:"Composition & Storytelling",description:"องค์ประกอบและเรื่องราว",maxScore:25},{name:"Creativity",description:"ความคิดสร้างสรรค์",maxScore:20},{name:"Technique & Completion",description:"เทคนิคและความสมบูรณ์",maxScore:15},{name:"Character Expression",description:"อารมณ์ตัวละคร",maxScore:10}],
  Screenshot:[{name:"Theme",description:"ความตรงตามธีม",maxScore:30},{name:"Composition",description:"องค์ประกอบภาพ",maxScore:30},{name:"Originality",description:"มุมมองที่แตกต่าง",maxScore:25},{name:"Polish",description:"ความเรียบร้อย",maxScore:15}],
  Cosplay:[{name:"Character Accuracy",description:"ความถูกต้องของตัวละคร",maxScore:30},{name:"Craftsmanship",description:"คุณภาพการสร้างชุด",maxScore:30},{name:"Creativity",description:"การตีความสร้างสรรค์",maxScore:20},{name:"Presentation",description:"การนำเสนอ",maxScore:20}],
  Video:[{name:"Story",description:"การเล่าเรื่อง",maxScore:25},{name:"Editing",description:"การตัดต่อ",maxScore:25},{name:"Creativity",description:"ความคิดสร้างสรรค์",maxScore:25},{name:"Theme",description:"ความตรงตามธีม",maxScore:25}],
};
export function CriteriaEditor({ initial }: { initial: Item[] }) {
  const [items,setItems]=useState(initial);
  const total=useMemo(()=>items.reduce((sum,item)=>sum+Number(item.maxScore||0),0),[items]);
  const set=(index:number,patch:Partial<Item>)=>setItems((current)=>current.map((item,i)=>i===index?{...item,...patch}:item));
  const move=(index:number,delta:number)=>setItems((current)=>{const next=[...current];const target=index+delta;if(target<0||target>=next.length)return current;[next[index],next[target]]=[next[target],next[index]];return next;});
  return <form action={createCriteriaVersionAction} className="space-y-4"><input type="hidden" name="criteriaJson" value={JSON.stringify(items)}/><div className="flex flex-wrap items-center gap-2 rounded-xl bg-sky-50 p-3"><span className="mr-1 text-xs font-black uppercase tracking-wider text-sky-700">Presets</span>{Object.entries(presets).map(([name,value])=><button key={name} type="button" onClick={()=>setItems(value.map(item=>({...item})))} className="focus-ring rounded-lg bg-white px-3 py-2 text-xs font-bold text-slate-600 shadow-sm hover:text-sky-700">{name}</button>)}</div><div className="space-y-3">{items.map((item,index)=><div key={`${index}-${item.name}`} className="grid gap-2 rounded-2xl border border-blue-100 bg-white p-4 sm:grid-cols-[1.2fr_1.4fr_110px_auto]"><Input aria-label="Criterion name" value={item.name} onChange={(event)=>set(index,{name:event.target.value})}/><Input aria-label="Description" value={item.description} onChange={(event)=>set(index,{description:event.target.value})}/><Input aria-label="Max score" type="number" min={1} max={100} value={item.maxScore} onChange={(event)=>set(index,{maxScore:Number(event.target.value)})}/><div className="flex gap-1"><button type="button" aria-label="Move up" onClick={()=>move(index,-1)} className="rounded-lg p-2 hover:bg-blue-50"><ArrowUp size={16}/></button><button type="button" aria-label="Move down" onClick={()=>move(index,1)} className="rounded-lg p-2 hover:bg-blue-50"><ArrowDown size={16}/></button><button type="button" aria-label="Duplicate" onClick={()=>setItems((current)=>[...current.slice(0,index+1),{...item,name:`${item.name} copy`},...current.slice(index+1)])} className="rounded-lg p-2 hover:bg-blue-50"><Copy size={16}/></button><button type="button" aria-label="Delete" onClick={()=>setItems((current)=>current.filter((_,i)=>i!==index))} className="rounded-lg p-2 text-red-600 hover:bg-red-50"><Trash2 size={16}/></button></div></div>)}</div><div className={`rounded-2xl p-4 ${total===100?"bg-emerald-50 text-emerald-800":"bg-red-50 text-red-800"}`}><div className="flex flex-wrap items-center justify-between gap-3"><strong>{total===100?`🟢 ${total} / 100`:`🔴 ${total} / 100 · ${total<100?`อีก ${100-total} คะแนนจึงจะครบ 100`:`เกิน ${total-100} คะแนน`}`}</strong><Button type="button" variant="secondary" onClick={()=>setItems((current)=>[...current,{name:"New criterion",description:"",maxScore:0}])}><Plus size={16}/> Add criteria</Button></div><div className="mt-3 grid gap-2 sm:grid-cols-[1fr_auto_auto]"><Input name="confirmation" placeholder="พิมพ์ CREATE VERSION" required/><Button name="migrationMode" value="NONE" type="submit" disabled={total!==100||items.length===0}>Create empty version</Button><Button name="migrationMode" value="COMPATIBLE" type="submit" variant="secondary" disabled={total!==100||items.length===0}>Migrate compatible scores</Button></div><p className="mt-2 text-xs">Migration จะสำเร็จต่อเมื่อชื่อเกณฑ์จับคู่ได้ครบและคะแนนเดิมไม่เกิน max ใหม่ มิฉะนั้นระบบจะ rollback ทั้งหมด</p></div></form>;
}
